---
description: Create a conventional git commit — delegates to the committer subagent.
disable-model-invocation: true
---

Delegate this task to the `committer` subagent: commit the current changes.
